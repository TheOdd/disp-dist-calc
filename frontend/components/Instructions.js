import React from 'react';

const Instructions = () => (
  <p>
    Please insert all applicable numbers into the fields below, and the result will update automatically.
  </p>
);

export default Instructions;
